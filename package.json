{
  "name": "cerberus",
  "version": "0.1.0",
  "description": "Remote control per sessioni Claude Code multi-account via tmux + Telegram",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.18"
  },
  "bin": {
    "cerberus": "./src/bin/cerberus.ts"
  },
  "scripts": {
    "dev": "node --env-file=.env --watch src/daemon/index.ts",
    "start": "node --env-file=.env src/daemon/index.ts",
    "typecheck": "tsc"
  },
  "dependencies": {
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
